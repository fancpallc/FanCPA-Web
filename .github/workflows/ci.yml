name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  docker-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run lint, build, and tests in Docker
        run: docker compose --profile test run --rm tests
